import {
  tedSelectIsEqual
} from '../../../helpers/ted-select-is-equal';
import { module, test } from 'qunit';

module('TedSelectIsEqualHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = tedSelectIsEqual(42);
  assert.ok(result);
});
